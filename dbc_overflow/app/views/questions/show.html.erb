<h1>Questions#show</h1>
<p>Find me in app/views/questions/show.html.erb</p>

<%= render 'answer_form' %>

<%= @question.title %>
<%= @question.content %>

<%= link_to 'Destroy', questions_path(@question),
  method: :delete,
  data: { confirm: 'Are you sure?' } %>
